spring:
  profiles:
    active: native
  application:
    name: config-service
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://your-git-repo-url
#          clone-on-start: true

eureka:
  client:
    enabled: false

server:
  port: 8888
